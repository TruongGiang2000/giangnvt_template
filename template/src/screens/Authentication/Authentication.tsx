import React from 'react';
import {View} from 'react-native';
import {AuthenticationProps} from './type';

const Authentication = (props: AuthenticationProps) => {
  console.log('props', props);
  return <View />;
};
export default Authentication;
